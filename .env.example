# Bitbucket API Configuration
# Get these values from your Bitbucket account:
# 1. Go to Bitbucket Settings → App passwords
# 2. Create a new App Password with 'Repositories: Read' permission
# 3. Use your workspace name and the generated app password

VITE_BITBUCKET_WORKSPACE=your-workspace-name
VITE_BITBUCKET_ACCESS_TOKEN=your-access-token