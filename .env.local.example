# Local Development Environment Variables
# Copy this file to .env.local and fill in your values
# .env.local is git-ignored and will override other .env files

VITE_BITBUCKET_WORKSPACE=your-workspace-name
VITE_BITBUCKET_ACCESS_TOKEN=your-access-token

# Note: In Vite, environment variables must be prefixed with VITE_ to be accessible in the frontend
# These will be available as import.meta.env.VITE_BITBUCKET_WORKSPACE and import.meta.env.VITE_BITBUCKET_ACCESS_TOKEN